env = {
    "HOST": "0.0.0.0",   # IP of your host
    "PORT": 8001,           # Port of your
    "MIN_CHAIN": 50,        # Minimum number of characters the string can have
    "MAX_CHAIN": 100,       # Maximum number of characters the string can have
    "MIN_SPACES": 3,        # Minimum number of spaces the string can have
    "MAX_SPACES": 5,        # Maximum number of spaces the string can have
    "CHAINS_FILE_NAME": "chains.txt",
    "RESPONSE_FILE_NAME": "response.txt",
    "SEPARATING_ELEMENT": ">>>"     # Separator element between the weight and the string, shown in the response file
}
